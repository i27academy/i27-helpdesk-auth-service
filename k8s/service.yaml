apiVersion: v1
kind: Service
metadata:
  name: i27-helpdesk-auth 
  namespace: ${NAMESPACE}
spec:
  selector:
    app: i27-helpdesk-auth
  ports:
  - name: http
    port: 80
    targetPort: 8081
  type: ClusterIP